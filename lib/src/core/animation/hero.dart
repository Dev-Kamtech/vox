/// Hero transition tag helpers.
///
/// Hero animations are accessed via the `.hero(tag)` extension on [Widget]
/// in `api/extensions.dart`. This file is intentionally minimal —
/// Flutter's built-in [Hero] widget handles all the heavy lifting.
library;
